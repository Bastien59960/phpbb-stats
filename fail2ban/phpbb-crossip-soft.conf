# Fail2Ban filter - cross-IP distributed download behavior (soft)
# Source log: /var/log/security_audit.log

[Definition]

failregex = ^.*PHPBB-XIP ip=<HOST> cc=(?!FR\b)[A-Z]{2} method=xip_dl_soft_v1 severity=soft score=\d+ .*$
ignoreregex =

[Init]

datepattern = ^%%Y-%%m-%%d %%H:%%M:%%S

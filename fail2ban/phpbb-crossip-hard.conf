# Fail2Ban filter - cross-IP distributed download behavior (hard)
# Source log: /var/log/security_audit.log

[Definition]

failregex = ^.*PHPBB-XIP ip=<HOST> cc=(?!FR\b)[A-Z]{2} method=xip_dl_hard_v1 severity=hard score=\d+ .*$
ignoreregex =

[Init]

datepattern = ^%%Y-%%m-%%d %%H:%%M:%%S
